#include <stdio.h>

int main()
{
    int x, y;
    
    x = 1500;
    y = 50;
    
    if ( x < 2000 || x > 3000)
    
        printf("Value of x is : %d\n", x);
    else
    {
        printf(" x isn't meet condition to be printed\n");
    }
    if ( y >= 100 && y <= 500)
    
        printf("Value of y is %d\n", y);
    else
    {
        printf(" y isn't meet condition to be printed\n");
    }
    
    
    return 0;
}
