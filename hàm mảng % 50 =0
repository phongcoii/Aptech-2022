#include <stdio.h>
int check(int num);

void main()
{
    int nhapso;
    int i = 0;
    int num[i];
    
   
    printf("Nhap so luong trong mang: ");
    scanf("%d", &nhapso);
    
    for (int i = 0; i < nhapso; i++)
    {
        {
        int check;
        int n;
       
        printf("num[%d]", i);
        scanf("%d", &num[i]);
        }
        if(num[i] % 5 ==0)
        {
            printf("\n%d divisible by 50", num[i]);
        }
        else
        {
            printf("non divisible by 50");
        }
        
    }
}
int check(int num)
{
    num [i] % 50 = 0;
    return 0;
}
