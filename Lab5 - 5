#include <stdio.h>

void main()
{
    int result = 0;
    int a;
    
    do
    {
        printf("Enter num : ");
        scanf("%d", &a);
        
    }while ( a >= 0 && a <= 100);
    result++;
    printf("Result is : %d\n", result);
    
}
