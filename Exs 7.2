#include <stdio.h>

void main()
{
    int a, b;
   
    printf("Enter Num 1 : ");
    scanf("%d", &a);
    
    printf("Enter Num 2 : ");
    scanf("%d", &b);
    
    if (a >= 1000 && b >= 1000){
        printf("Num 1 & Num 2 is equal or greater than 1000\n");
    }
    else if (a <= 1000 && b <= 1000){
        printf("Num 1 & Num 2 is less than 1000\n");
    }
    else if (a >= 1000 && b <= 1000){
        printf("Only Num 1 greather than 1000\n");
    }
    else
        printf("Only Num 2 greater than 1000\n");

}
