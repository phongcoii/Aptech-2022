#include <stdio.h>

int main()
{
    int a, b;
   
    printf("Enter A value :");
    scanf("%d", &a);
    
    printf("Enter B value :");
    scanf("%d", &b);
    
    if (a % b == 0)
        printf("A is divisible by b");
    else
        printf("A is not divisible by b");
    
    return 0;
}
