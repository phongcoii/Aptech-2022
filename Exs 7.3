#include <stdio.h>

void main()
{
    int num1, num2;
    int difference = 0;
    
   
    printf("Enter Num 1 : ");
    scanf("%d", &num1);
    
    printf("Enter Num 2 : ");
    scanf("%d", &num2);
    
    difference = num1 - num2;
    if ( difference == num1 || difference == num2)
    {
        
        if (num1 == difference)
            printf("\nDifference is equal to num 1\n");
        else if (num2 == difference)
            printf("\nDifference is equal to num 2\n");
    }
   
    
    else if ( difference != num1 && difference != num2 )
            printf("Difference is not equal to any value entered\n");
            printf("Difference is equal to %d\n", difference);
    
}
